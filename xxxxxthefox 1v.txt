-- Services
local Players = game:GetService("Players")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- URLs
local ArabicURL = "https://raw.githubusercontent.com/xxxxxthefox/hhgg/refs/heads/main/m"
local EnglishURL = "https://raw.githubusercontent.com/xxxxxthefox/hhgg/refs/heads/main/F"

-- GUI Setup
local gui = Instance.new("ScreenGui")
gui.Name = "LanguageGUI"
gui.Parent = playerGui
gui.ResetOnSpawn = false

local main = Instance.new("Frame", gui)
main.Size = UDim2.new(0, 400, 0, 260)
main.Position = UDim2.new(0.5, 0, 0.5, 0)
main.AnchorPoint = Vector2.new(0.5, 0.5)
main.BackgroundColor3 = Color3.fromRGB(40, 120, 40)
main.BorderSizePixel = 0
Instance.new("UICorner", main).CornerRadius = UDim.new(0, 12)

local title = Instance.new("TextLabel", main)
title.Size = UDim2.new(1, -20, 0, 50)
title.Position = UDim2.new(0, 10, 0, 10)
title.BackgroundTransparency = 1
title.Font = Enum.Font.GothamBold
title.TextColor3 = Color3.new(1, 1, 1)
title.TextScaled = true
title.Text = "🌍 اختر اللغة / Choose Language"

local container = Instance.new("Frame", main)
container.Size = UDim2.new(1, -40, 0, 130)
container.Position = UDim2.new(0, 20, 0, 70)
container.BackgroundTransparency = 1

local layout = Instance.new("UIListLayout", container)
layout.SortOrder = Enum.SortOrder.LayoutOrder
layout.Padding = UDim.new(0, 12)
layout.HorizontalAlignment = Enum.HorizontalAlignment.Center

-- زر الخروج
local exitButton = Instance.new("TextButton", main)
exitButton.Size = UDim2.new(0, 35, 0, 35)
exitButton.Position = UDim2.new(1, -40, 0, 5)
exitButton.Text = "X"
exitButton.Font = Enum.Font.GothamBold
exitButton.TextScaled = true
exitButton.TextColor3 = Color3.new(1, 1, 1)
exitButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
Instance.new("UICorner", exitButton).CornerRadius = UDim.new(1, 0)

exitButton.MouseButton1Click:Connect(function()
	gui:Destroy()
end)

-- Function to create button
local function AddButton(text, url)
	local btn = Instance.new("TextButton", container)
	btn.Size = UDim2.new(1, 0, 0, 50)
	btn.BackgroundColor3 = Color3.fromRGB(55, 55, 55)
	btn.Text = text
	btn.Font = Enum.Font.Gotham
	btn.TextColor3 = Color3.new(1, 1, 1)
	btn.TextScaled = true
	Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 10)

	btn.MouseButton1Click:Connect(function()
		gui:Destroy()
		local success, result = pcall(function()
			return game:HttpGet(url)
		end)
		if success then
			loadstring(result)()
		else
			warn("فشل تحميل السكربت: " .. tostring(result))
		end
	end)
end

-- إنشاء الأزرار
AddButton("العربية", ArabicURL)
AddButton("English", EnglishURL)
