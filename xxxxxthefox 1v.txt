-- ✅ إعدادات عامة
getgenv().settings = {
    enable_aimbot = true,
    enable_prediction = true,
    enable_protection = true,
    teamCheck = true,
    wallCheck = true,
    predictionFactor = 0.1,
    minWait = 0.00001,
    maxWait = 0.00005,
    detectionPrecision = 0.00001,
    fov = 30,
    drawFov = true,
    third_person_enabled = false,
    super_rotation_enabled = false,
}

-- ✅ تحميل المكتبات
local aimbot = loadstring(game:HttpGet("https://raw.githubusercontent.com/xxxxxthefox/My-library/refs/heads/main/oo"))()
local ESP = loadstring(game:HttpGet("https://raw.githubusercontent.com/xxxxxthefox/1fox1/refs/heads/main/1z1"))()
local uiLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/tbao143/Library-ui/refs/heads/main/Redzhubui"))()

-- ✅ إعداد مكتبة ESP
ESP.Enabled = true
ESP.ShowBox = true
ESP.BoxType = "Corner Box Esp"
ESP.ShowName = true
ESP.ShowHealth = true
ESP.ShowTracer = true
ESP.ShowDistance = true

-- ✅ واجهة المستخدم
local Window = uiLib:MakeWindow({
    Title = "xxxxxTheFox | Aimbot & ESP",
    SubTitle = "الإصدار الرسمي",
    SaveFolder = "xxxxxTheFox_Config"
})

Window:AddMinimizeButton({
    Button = { Image = "rbxassetid://115469660765124", BackgroundTransparency = 0 },
    Corner = { CornerRadius = UDim.new(35, 1) }
})

--------------------------
-- 📌 تبويب الأيمبوت
--------------------------
local AimbotTab = Window:MakeTab({"🎯 الأيمبوت", "Aimbot"})
Window:SelectTab(AimbotTab)

AimbotTab:AddSection({"⚙️ تفعيل الميزات"})

local toggleSettings = {
    { "✅ تفعيل الأيمبوت", "enable_aimbot" },
    { "🎯 التنبؤ بالهدف", "enable_prediction" },
    { "🛡️ الحماية من الكشف", "enable_protection" },
    { "👥 التحقق من الفريق", "teamCheck" },
    { "🧱 التحقق من الحائط", "wallCheck" },
    { "🌀 عرض دائرة FOV", "drawFov" }
}

for _, t in pairs(toggleSettings) do
    AimbotTab:AddToggle({
        Name = t[1],
        Default = getgenv().settings[t[2]],
        Callback = function(val)
            getgenv().settings[t[2]] = val
        end
    })
end

AimbotTab:AddSection({"🔢 إعدادات رقمية"})

local numberSettings = {
    { "📐 زاوية FOV", "fov", "حدد زاوية التصويب (مثال: 30)" },
    { "🔮 قوة التنبؤ", "predictionFactor", "أدخل رقم مثل 0.1" },
    { "⏱️ الحد الأدنى بين الضربات", "minWait", "مثال: 0.00001" },
    { "⏲️ الحد الأقصى بين الضربات", "maxWait", "مثال: 0.00005" },
    { "🎯 دقة التصويب", "detectionPrecision", "كلما قل الرقم زادت الدقة" }
}

for _, s in pairs(numberSettings) do
    AimbotTab:AddTextBox({
        Name = s[1],
        Description = s[3],
        PlaceholderText = tostring(getgenv().settings[s[2]]),
        Callback = function(val)
            local num = tonumber(val)
            if num then
                getgenv().settings[s[2]] = num
            else
                warn("❌ أدخل رقم صحيح")
            end
        end
    })
end

AimbotTab:AddButton({
    Name = "🔁 إعادة تحميل الأيمبوت",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/xxxxxthefox/My-library/refs/heads/main/oo"))()
        game.StarterGui:SetCore("SendNotification", {
            Title = "xxxxxTheFox",
            Text = "✅ تم إعادة تحميل الأيمبوت",
            Duration = 5
        })
    end
})

--------------------------
-- 📌 تبويب الرؤية (ESP)
--------------------------
local ESPTab = Window:MakeTab({"👁️ الرؤية", "ESP"})

ESPTab:AddSection({"🎯 إعدادات ESP"})

local espToggles = {
    { "✅ تفعيل ESP", "Enabled" },
    { "📦 إظهار الصناديق", "ShowBox" },
    { "🔠 إظهار الأسماء", "ShowName" },
    { "❤️ إظهار الصحة", "ShowHealth" },
    { "📍 إظهار الخطوط", "ShowTracer" },
    { "📏 إظهار المسافة", "ShowDistance" },
}

for _, t in pairs(espToggles) do
    ESPTab:AddToggle({
        Name = t[1],
        Default = ESP[t[2]],
        Callback = function(val)
            ESP[t[2]] = val
        end
    })
end

ESPTab:AddDropdown({
    Name = "🧩 نوع الصندوق",
    Options = {"2D", "Corner Box Esp", "Highlight"},
    Default = ESP.BoxType,
    Callback = function(opt)
        ESP.BoxType = opt
    end
})

--------------------------
-- 📌 تبويب الحركات الخاصة
--------------------------
local MotionTab = Window:MakeTab({"🎮 الحركات", "Activations"})
MotionTab:AddSection({"🌀 الميزات المتقدمة"})

-- منظور ثالث
local function toggleThirdPerson(state)
    local player = game:GetService("Players").LocalPlayer
    local camera = workspace.CurrentCamera

    if state then
        camera.CameraType = Enum.CameraType.Custom
        player.CameraMode = Enum.CameraMode.Classic
    else
        camera.CameraType = Enum.CameraType.Custom
        player.CameraMode = Enum.CameraMode.LockFirstPerson
    end
end

-- دوران مجنون
local RunService = game:GetService("RunService")
local player = game:GetService("Players").LocalPlayer
local rotationSpeed = 99999999
local hrp = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
local rotationConnection

player.CharacterAdded:Connect(function(char)
    task.wait(0.5)
    hrp = char:WaitForChild("HumanoidRootPart")
end)

local function startSuperRotation()
    if rotationConnection then return end
    rotationConnection = RunService.RenderStepped:Connect(function(dt)
        if getgenv().settings.super_rotation_enabled and hrp and hrp.Parent then
            local angle = math.rad(rotationSpeed) * dt
            hrp.CFrame = hrp.CFrame * CFrame.Angles(0, angle, 0)
        end
    end)
end

local function stopSuperRotation()
    if rotationConnection then
        rotationConnection:Disconnect()
        rotationConnection = nil
    end
end

-- تفعيل منظور ثالث
MotionTab:AddToggle({
    Name = "📸 تفعيل منظور ثالث",
    Default = getgenv().settings.third_person_enabled,
    Callback = function(state)
        getgenv().settings.third_person_enabled = state
        toggleThirdPerson(state)
    end
})

-- تفعيل دوران مجنون
MotionTab:AddToggle({
    Name = "🔁 تفعيل دوران مجنون",
    Default = getgenv().settings.super_rotation_enabled,
    Callback = function(state)
        getgenv().settings.super_rotation_enabled = state
        if state then startSuperRotation() else stopSuperRotation() end
    end
})

MotionTab:AddButton({
    Name = "🚀 تفعيل كل الميزات المجنونة",
    Callback = function()
        getgenv().settings.third_person_enabled = true
        getgenv().settings.super_rotation_enabled = true
        toggleThirdPerson(true)
        startSuperRotation()
    end
})

-- ✅ إشعار التحميل
pcall(function()
    game.StarterGui:SetCore("SendNotification", {
        Title = "xxxxxTheFox",
        Text = "✅ تم تحميل الواجهة بنجاح مع جميع الميزات!",
        Duration = 5
    })
end)
